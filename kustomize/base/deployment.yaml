apiVersion: apps/v1
kind: Deployment
metadata:
  name: timelog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timelog
      version: v1
  template:
    metadata:
      labels:
        app: timelog
        version: v1
    spec:
      containers:
        - name: timelog
          image: nightlord851108/timelog:master.f320057
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
